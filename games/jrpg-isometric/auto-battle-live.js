import * as THREE from'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';import{createParty}from'./party-system-v1.js';import{createAutoBattle,ROLES}from'./auto-battle-m1.js';const view=document.getElementById('view'),scene=new THREE.Scene(),renderer=new THREE.WebGLRenderer({antialias:true}),camera=new THREE.OrthographicCamera(-12,12,8,-8,.1,100),debug=new THREE.Group(),spatial={target:true,candidates:true,chosen:true,threat:true};renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));view.appendChild(renderer.domElement);scene.background=new THREE.Color(0x84978c);camera.position.set(16,18,16);camera.lookAt(0,0,0);scene.add(new THREE.HemisphereLight(0xffffff,0x334433,2),debug);const ground=new THREE.Mesh(new THREE.CylinderGeometry(9,9,.3,64),new THREE.MeshStandardMaterial({color:0x4f6b45}));ground.position.y=-.15;scene.add(ground);const specs=[['p1','อัศวิน',-4.8,-1.5,0x285c91,620,58,112,90],['p2','นักเวท',-5.2,1.6,0x684c9c,470,70,98,65],['e1','ซามูไร',4.8,1.4,0x8a2b20,520,78,108,76],['e2','นักฆ่า',5.2,-1.6,0x54202d,450,88,102,62]];let actors=[],battle,timer,speed=1,animating=false,selectedActor='e1',selectedDecision=null;
const A=id=>actors.find(a=>a.id===id),pct=v=>Math.round((+v||0)*100)+'%',num=v=>(+v||0).toFixed(1);function mk(s){let[id,name,x,z,color,hp,spd,atk,def]=s;return{id,name,team:id[0],x,z,color,hp,maxHP:hp,spd,atk,def,accuracy:.84,evasion:.08,crit:.09,posture:100,maxPosture:100,stamina:100,maxStamina:100,mp:55,maxMP:55,move:3.8,zoc:1.75}}function vis(a){const g=new THREE.Group(),m=new THREE.Mesh(new THREE.CylinderGeometry(.3,.38,.9,8),new THREE.MeshStandardMaterial({color:a.color}));m.position.y=.5;g.add(m);scene.add(g);a.model=g;g.position.set(a.x,.05,a.z)}function clearDebug(){while(debug.children.length){const o=debug.children.pop();o.geometry?.dispose();o.material?.dispose()}}function ring(x,z,r,c,o=.8){const m=new THREE.Mesh(new THREE.RingGeometry(r,r+.07,28),new THREE.MeshBasicMaterial({color:c,transparent:true,opacity:o,side:THREE.DoubleSide,depthTest:false}));m.rotation.x=-Math.PI/2;m.position.set(x,.09,z);debug.add(m)}function line(a,b,c){debug.add(new THREE.Line(new THREE.BufferGeometry().setFromPoints([new THREE.Vector3(a.x,.1,a.z),new THREE.Vector3(b.x,.1,b.z)]),new THREE.LineBasicMaterial({color:c,depthTest:false})))}
function spatialRender(s){clearDebug();if(!s)return;const t=A(s.chosen.target),moves=s.candidates?.filter(c=>c.type==='MOVE')||[];if(spatial.target&&t)ring(t.x,t.z,.68,0xffd45c);if(spatial.threat)for(const q of(s.cognition?.threats||[])){const a=A(q.id);if(a)ring(a.x,a.z,.75,0xff493d,.35+.5*(q.score||0))}if(spatial.candidates)moves.forEach(c=>ring(c.x,c.z,.24,0x73c9ff,.4));const ch=moves.find(c=>c.label===s.chosen.movement)||moves[0];if(spatial.chosen&&ch){ring(ch.x,ch.z,.38,0x72ff9d);line(s.observation.self,ch,0x72ff9d);if(t)line(ch,t,0xffd45c)}}
function renderCards(){const r=document.getElementById('cards');r.innerHTML='';actors.forEach(a=>{const d=document.createElement('div');d.className='card'+(a.id===selectedActor?' selected':'');d.innerHTML=`<b>${a.name}</b><div class=muted>${ROLES[a.id]}</div><div class=bar><div class=hp style="width:${100*a.hp/a.maxHP}%"></div></div>HP ${Math.round(a.hp)} · ST ${Math.round(a.stamina)}`;d.onclick=()=>{selectedActor=a.id;selectedDecision=battle.inspector.latest(a.id)?.decisionId;renderAll()};r.appendChild(d)})}
function role(s){const c=s?.cognition;if(!c)return'—';if(s.actorId==='p1')return`Protect ${c.protect?.ally?.id||'-'} · Intercept ${num(c.intercept?.score)} · Control ${num(c.control?.[0]?.score)}`;if(s.actorId==='p2')return`Cast Safety ${pct(c.castSafety?.safety)} · Reserve ${pct(c.mana?.reserveTarget)} · Escape ${pct(c.escape?.score)}`;if(s.actorId==='e1')return`Commit ${pct(c.timing?.[0]?.commit)} · Parry ${pct(c.counter?.[0]?.parryConfidence)} · Tempo ${pct(c.tempo?.tempo)}`;return`Isolation ${pct(c.isolations?.[0]?.isolation)} · Flank ${c.flank?.side||'-'} · Execute ${pct(c.execute?.chance)}`}
function renderInspector(s){if(!s)return;selectedActor=s.actorId;selectedDecision=s.decisionId;spatialRender(s);const a=A(s.actorId);document.getElementById('insActor').textContent=`🤖 ${a?.name} — ${ROLES[s.actorId]}`;document.getElementById('insBrain').textContent=s.brainVersion;document.getElementById('insIntent').textContent=s.intent.name;document.getElementById('insConfidence').textContent=`${pct(s.intent.confidence)} · Decision #${s.decisionId}`;document.getElementById('insChosen').textContent=`${s.chosen.movement} → ${s.chosen.action} → ${s.chosen.target} · U ${num(s.chosen.utility)}`;document.getElementById('insReason').textContent=s.intent.reason||'-';const q=s.observation.self;document.getElementById('insState').textContent=`HP ${Math.round(q.hp)} · ST ${Math.round(q.stamina)} · PO ${Math.round(q.posture)} · MP ${Math.round(q.mp||0)}`;document.getElementById('insTeam').textContent=s.teamContext?`${s.teamContext.intent} → ${s.teamContext.target}`:'-';document.getElementById('insPerf').textContent=`${s.performance.candidateCount} candidates · ${num(s.performance.timeMs)} ms`;document.getElementById('rolePanel').innerHTML=`<div class=panel>${role(s)}</div>`;document.getElementById('candidateList').innerHTML=(s.candidates||[]).map((c,i)=>`<div class=candidate><b>${i+1}. ${c.label||c.key}</b> · ${num(c.utility)}</div>`).join('');document.getElementById('traceRaw').textContent=`Round ${s.round} · Decision #${s.decisionId}\n${s.actorId}: ${s.intent.name}\n${s.chosen.movement} → ${s.chosen.action} → ${s.chosen.target}`;renderCards()}
function renderTimeline(){const all=battle.inspector.all(),root=document.getElementById('timeline');document.getElementById('timelineCount').textContent=`${all.length} decisions`;root.innerHTML='';for(const s of all){const a=A(s.actorId),b=document.createElement('button');b.className='decisionDot'+(s.decisionId===selectedDecision?' active':'');b.innerHTML=`<div class=round>R${s.round} · #${s.decisionId}</div><b>${a?.name||s.actorId}</b><div class=intent>${s.intent.name}</div>`;b.onclick=()=>{stop();renderInspector(s);renderTimeline()};root.appendChild(b)}const active=root.querySelector('.active');active?.scrollIntoView({inline:'center',block:'nearest'})}function renderAll(){const s=selectedDecision?battle.inspector.all().find(x=>x.decisionId===selectedDecision):battle.inspector.latest(selectedActor);if(s)renderInspector(s);renderTimeline()}
function reset(){stop();clearDebug();actors.forEach(a=>a.model&&scene.remove(a.model));actors=specs.map(mk);actors.forEach(vis);battle=createAutoBattle(createParty('p',actors.filter(a=>a.team==='p')),createParty('e',actors.filter(a=>a.team==='e')));selectedDecision=null;renderCards();renderTimeline()}async function animate(before,f){animating=true;document.getElementById('blueIntent').textContent=f.teamPlans.player.intent;document.getElementById('redIntent').textContent=f.teamPlans.enemy.intent;const st=performance.now();await new Promise(done=>{function q(n){const t=Math.min(1,(n-st)/(550/speed));actors.forEach(a=>{const b=before.get(a.id)||a;a.model.position.set(b.x+(a.x-b.x)*t,.05,b.z+(a.z-b.z)*t)});t<1?requestAnimationFrame(q):done()}requestAnimationFrame(q)});selectedDecision=battle.inspector.latest(selectedActor)?.decisionId;renderAll();document.getElementById('log').textContent=`Round ${f.round} · `+f.plans.map(p=>`${p.actor}:${p.intent}`).join(' | ');animating=false}function step(){if(animating)return;const b=new Map(actors.map(a=>[a.id,{x:a.x,z:a.z}]));animate(b,battle.step())}function play(){if(timer)return;battle.play();timer=setInterval(step,Math.max(500,1800/speed));step()}function stop(){if(timer){clearInterval(timer);timer=null}battle?.pause()}
document.getElementById('play').onclick=play;document.getElementById('pause').onclick=stop;document.getElementById('step').onclick=()=>{stop();step()};document.getElementById('restart').onclick=reset;document.querySelectorAll('[data-speed]').forEach(b=>b.onclick=()=>{speed=+b.dataset.speed;document.querySelectorAll('[data-speed]').forEach(x=>x.classList.toggle('on',x===b))});document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.toggle('on',x===b));['overview','role','candidates','trace'].forEach(t=>document.getElementById('tab-'+t).classList.toggle('hidden',b.dataset.tab!==t))});document.querySelectorAll('[data-spatial]').forEach(b=>b.onclick=()=>{spatial[b.dataset.spatial]=!spatial[b.dataset.spatial];b.classList.toggle('on',spatial[b.dataset.spatial]);renderAll()});function globalStep(delta){stop();const all=battle.inspector.all(),i=all.findIndex(s=>s.decisionId===selectedDecision),n=Math.max(0,Math.min(all.length-1,(i<0?all.length-1:i)+delta));if(all[n]){renderInspector(all[n]);renderTimeline()}}document.getElementById('prevDecision').onclick=()=>globalStep(-1);document.getElementById('nextDecision').onclick=()=>globalStep(1);document.getElementById('latestDecision').onclick=()=>{stop();const a=battle.inspector.all();if(a.length){renderInspector(a[a.length-1]);renderTimeline()}};function resize(){const w=view.clientWidth,h=view.clientHeight;renderer.setSize(w,h,false);const f=16,asp=w/h;camera.left=-f*asp/2;camera.right=f*asp/2;camera.top=f/2;camera.bottom=-f/2;camera.updateProjectionMatrix()}new ResizeObserver(resize).observe(view);resize();reset();(function loop(){renderer.render(scene,camera);requestAnimationFrame(loop)})();
