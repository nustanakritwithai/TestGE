// Mobile-first party UI. Can be mounted by the future 2v2 engine without touching renderer internals.
export function mountPartyUI(root,{onActor,onTarget}={}){const el=document.createElement('section');el.id='party2v2';el.className='panel';el.innerHTML='<b>2v2 Party</b><div id="partyActors" style="display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-top:7px"></div><div style="margin-top:8px"><b>เป้าหมาย</b><div id="partyTargets" style="display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-top:6px"></div></div>';root.appendChild(el);return{render(playerParty,enemyParty,state){const pa=el.querySelector('#partyActors'),pt=el.querySelector('#partyTargets');pa.innerHTML='';pt.innerHTML='';for(const a of playerParty.members){const b=document.createElement('button');b.textContent=`${a.name} · HP ${Math.max(0,Math.round(a.hp))}`;b.disabled=a.hp<=0||state.committed.has(a.id);b.dataset.selected=state.activeActorId===a.id;b.onclick=()=>onActor?.(a.id);pa.appendChild(b);}for(const a of enemyParty.members){const b=document.createElement('button');b.textContent=`${a.name} · HP ${Math.max(0,Math.round(a.hp))}`;b.disabled=a.hp<=0;b.dataset.selected=state.targetId===a.id;b.onclick=()=>onTarget?.(a.id);pt.appendChild(b);}}};}
