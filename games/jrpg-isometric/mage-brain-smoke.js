import{createMageDecision}from'./mage-brain-v1.js';const a=(id,hp=100,mp=50,x=0,z=0,atk=100)=>({id,hp,maxHP:100,mp,maxMP:50,stamina:100,maxStamina:100,posture:100,maxPosture:100,x,z,atk});
export function smokeMageBrain(){const ally=a('p1',100,20,-1,0),close=a('e2',100,20,.8,0,115);let m=a('p2',100,50,0,0),unsafe=createMageDecision(m,[m,ally],[close]);m=a('p2',100,5,0,0);const conserve=createMageDecision(m,[m,ally],[a('e1',100,20,4,0)]);m=a('p2',100,50,0,0);const safe=createMageDecision(m,[m,ally],[a('e1',100,20,3.5,0)]);m=a('p2',15,30,0,0);const escape=createMageDecision(m,[m,ally],[close]);return{pass:unsafe.intent==='REPOSITION_SAFE'&&conserve.intent==='CONSERVE_MANA'&&safe.intent==='SAFE_CAST'&&escape.intent==='EMERGENCY_ESCAPE',unsafe:unsafe.intent,conserve:conserve.intent,safe:safe.intent,escape:escape.intent};}
