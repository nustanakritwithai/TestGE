// R11.5 Quality Gate — evaluates fixed-size validation summaries; no battle authority.
export const QUALITY={minDecisions:12,maxUnsafe:0,maxChangeRate:.35,maxDrift:.1,minUpdates:6};
export function evaluateShadowQuality(summary={}){const reasons=[];if((summary.decisions||0)<QUALITY.minDecisions)reasons.push('INSUFFICIENT_DECISIONS');if((summary.updates||0)<QUALITY.minUpdates)reasons.push('INSUFFICIENT_UPDATES');if((summary.unsafe||0)>QUALITY.maxUnsafe)reasons.push('UNSAFE_VALUES');if((summary.changeRate||0)>QUALITY.maxChangeRate)reasons.push('TOO_VOLATILE');if((summary.maxAbsDrift||0)>QUALITY.maxDrift)reasons.push('EXCESSIVE_DRIFT');return{pass:reasons.length===0,reasons,metrics:{decisions:summary.decisions||0,updates:summary.updates||0,changeRate:summary.changeRate||0,unsafe:summary.unsafe||0,maxAbsDrift:summary.maxAbsDrift||0},thresholds:{...QUALITY}}}
export function evaluateBattleQuality(summaries={}){const actors=Object.fromEntries(Object.entries(summaries).map(([id,s])=>[id,evaluateShadowQuality(s)])),ready=Object.values(actors).length>0&&Object.values(actors).every(x=>x.pass);return{ready,actors}}
