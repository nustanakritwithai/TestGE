// R11.2 Shadow Mode — observes candidates and self-outcome memory but NEVER changes chosen action.
import{runAdaptationLab,comparePolicy}from'./adaptation-lab-v1.js';
export function shadowAdaptation(memory,candidates=[]){const actions=candidates.filter(c=>c?.type==='ACTION'&&Number.isFinite(c.utility)),samples=[];for(const x of memory?.self||[]){samples.push({action:x.action,attempts:x.attempts||0,confidence:Math.min(1,1-Math.exp(-(x.attempts||0)/5)),predictionError:(x.totalRealizedUtility-x.totalExpectedUtility)/Math.max(1,x.attempts||1)})}const lab=runAdaptationLab(samples),base=Object.fromEntries(actions.map(x=>[x.key,x.utility])),rank=comparePolicy(base,lab.weights),baseline=actions.slice().sort((a,b)=>b.utility-a.utility)[0]?.key||null,shadow=rank[0]?.action||baseline;return{schema:'TestGE/AdaptationShadow/v1',baselineAction:baseline,shadowAction:shadow,wouldChange:!!baseline&&!!shadow&&baseline!==shadow,weights:lab.weights,rank:rank.slice(0,6),safe:lab.pass}}
