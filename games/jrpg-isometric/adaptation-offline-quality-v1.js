// Post-battle only. Converts offline validation into an auditable readiness report.
export const OFFLINE_GATE={minFrames:6,minPlans:16,minActorDecisions:4,maxUnsafe:0,maxChangeRate:.4};
export function offlineQualityReport(validation={}){const reasons=[],stats=validation.stats||{};if((stats.frames||0)<OFFLINE_GATE.minFrames)reasons.push('TOO_SHORT');if((stats.plans||0)<OFFLINE_GATE.minPlans)reasons.push('TOO_FEW_PLANS');if((stats.unsafe||0)>OFFLINE_GATE.maxUnsafe)reasons.push('UNSAFE');const actors={};for(const[id,s]of Object.entries(validation.summaries||{})){const r=[];if((s.decisions||0)<OFFLINE_GATE.minActorDecisions)r.push('LOW_EVIDENCE');if((s.changeRate||0)>OFFLINE_GATE.maxChangeRate)r.push('VOLATILE');if((s.unsafe||0)>0)r.push('UNSAFE');actors[id]={pass:r.length===0,reasons:r,decisions:s.decisions||0,updates:s.updates||0,changeRate:s.changeRate||0,weights:s.weights||{}};if(r.length)reasons.push(`${id}:${r.join('+')}`)}return{schema:'TestGE/R11OfflineQuality/v1',pass:reasons.length===0,reasons,stats,actors,thresholds:{...OFFLINE_GATE}}}
